const Home = () => {
  return (
    <main className="flex min-h-screen bg-black max-w-full overflow-x-hidden">
      <canvas id="starCanvas" className="absolute top-0 left-0"></canvas>
      <script src="script.js"></script>
    </main>
  );
}

export default Home;
