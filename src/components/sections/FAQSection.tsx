import React, { FC } from 'react';
import {
  Accordion,
  AccordionContent,
  AccordionItem,
  AccordionTrigger,
} from '@/components/shared/accordion/Accordion';

interface FAQSectionProps {
  FAQ: { question: string; answer: string }[];
}

const FAQSection: FC<FAQSectionProps> = ({ FAQ }) => {
  return
  ));
};

export default FAQSection;
